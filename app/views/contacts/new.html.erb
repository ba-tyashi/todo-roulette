<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-md-6 col-lg-5">
      <div class="card shadow-sm border-0">
        <div class="card-body p-4">
          <h2 class="text-center mb-4" style="font-size: 1.5rem; font-weight: bold;">管理者への問い合わせ</h2>
          
          <p class="text-muted small mb-4 text-center">
            パスワードの再発行をご希望の方は、登録時のメールアドレスを入力して送信してください。
          </p>

          <%= form_with model: @contact, local: true do |f| %>
            <div class="mb-3">
              <%= f.label :email, "登録済みのメールアドレス", class: "form-label" %>
              <%= f.email_field :email, class: "form-control form-control-lg", required: true, placeholder: "example@gmail.com" %>
            </div>
            
            <div class="mb-4">
              <%= f.label :content, "お問い合わせ内容", class: "form-label" %>
              <%= f.text_area :content, class: "form-control", rows: 4, value: "パスワードの再発行をお願いします。" %>
            </div>

            <div class="d-grid mb-3">
              <%= f.submit "送信する", class: "btn btn-primary btn-lg" %>
            </div>
          <% end %>

          <div class="text-center">
            <hr class="my-4">
            <%= link_to "ログイン画面に戻る", new_user_session_path, class: "text-decoration-none small text-secondary" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>