<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-md-6 col-lg-5">
      
      <%# デザイン %>
      <div class="card shadow-sm border">
        <div class="card-body p-4">
          <h2 class="text-center mb-4" style="font-weight: bold;">管理者への問い合わせ</h2>
          
          <p class="text-muted small mb-4">
            パスワードの再発行をご希望の方は、登録時のメールアドレスを入力して送信してください。
          </p>

          <%= form_with model: @contact, local: true do |f| %>
            
            <%# メールアドレス入力欄 %>
            <div class="mb-3">
              <%= f.label :email, "メールアドレス", class: "form-label fw-bold" %>
              <%= f.email_field :email, class: "form-control", required: true, autofocus: true, placeholder: "email@example.com" %>
            </div>
            
            <%# 内容入力欄（初期値を設定済み） %>
            <div class="mb-4">
              <%= f.label :content, "お問い合わせ内容", class: "form-label fw-bold" %>
              <%= f.text_area :content, class: "form-control", rows: 3, value: "パスワードの再発行をお願いします。" %>
            </div>

            <%# 青色の送信ボタン %>
            <div class="d-grid gap-2">
              <%= f.submit "送信する", class: "btn btn-primary btn-lg" %>
            </div>

          <% end %>

          <hr class="my-4">

          <div class="text-center">
            <%= link_to "ログイン画面に戻る", new_user_session_path, class: "text-decoration-none small" %>
          </div>
        </div>
      </div>

    </div>
  </div>
</div>