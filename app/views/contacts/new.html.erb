<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <div class="card shadow-sm">
        <div class="card-header bg-primary text-white">
          <h2 class="h5 mb-0">管理者への問い合わせ</h2>
        </div>
        <div class="card-body">
          <p class="text-muted small">パスワードの再発行をご希望の方は、登録時のメールアドレスを入力して送信してください。</p>
          
          <%= form_with model: @contact, local: true do |f| %>
            <div class="mb-3">
              <%= f.label :email, "登録済みのメールアドレス", class: "form-label fw-bold" %>
              <%= f.email_field :email, class: "form-control", required: true, placeholder: "example@gmail.com" %>
            </div>
            
            <div class="mb-4">
              <%= f.label :content, "お問い合わせ内容", class: "form-label fw-bold" %>
              <%= f.text_area :content, class: "form-control", rows: 4, value: "パスワードの再発行をお願いします。" %>
            </div>

            <div class="d-grid">
              <%= f.submit "送信する", class: "btn btn-primary btn-lg" %>
            </div>
          <% end %>
        </div>
        <div class="card-footer text-center">
          <%= link_to "ログイン画面に戻る", new_user_session_path, class: "text-decoration-none small" %>
        </div>
      </div>
    </div>
  </div>
</div>