<div class="container mt-5">
  <div class="row justify-content-center">
    <div class="col-md-5">
      
      <%# ログイン画面のようなカード形式のデザイン %>
      <div class="card shadow-sm p-4" style="border-radius: 10px; border: 1px solid #f0f0f0;">
        <div class="card-body">
          <h2 class="text-center mb-4" style="font-weight: bold; color: #333;">管理者への問い合わせ</h2>
          
          <p class="text-muted small mb-4 text-center">
            パスワードの再発行をご希望の方は、<br>メールアドレスを入力して送信してください。
          </p>

          <%= form_with model: @contact, local: true do |f| %>
            
            <%# メールアドレス欄：ラベルと入力欄を縦に揃える %>
            <div class="mb-3 text-start">
              <%= f.label :email, "メールアドレス", class: "form-label fw-bold small" %>
              <%= f.email_field :email, class: "form-control", required: true, style: "border: 1.5px solid #333; border-radius: 8px; padding: 10px;" %>
            </div>
            
            <%# お問い合わせ内容欄：ここも縦に揃えます %>
            <div class="mb-4 text-start">
              <%= f.label :content, "お問い合わせ内容", class: "form-label fw-bold small" %>
              <%= f.text_area :content, class: "form-control", rows: 3, value: "パスワードの再発行をお願いします。", style: "border: 1.5px solid #ddd; border-radius: 8px; background-color: #f0f4ff;" %>
            </div>

            <%# 送信ボタン：青色に変更 %>
            <div class="d-grid gap-2">
              <%= f.submit "送信する", class: "btn btn-primary fw-bold", style: "background-color: #1a73e8; border: none; padding: 12px; border-radius: 8px;" %>
            </div>

          <% end %>

          <div class="text-center mt-4 pt-2" style="border-top: 1px solid #eee;">
            <%= link_to "ログイン画面に戻る", new_user_session_path, class: "text-decoration-none small", style: "color: #1a73e8; font-weight: bold;" %>
          </div>
        </div>
      </div>

    </div>
  </div>
</div>